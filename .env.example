# Google Calendar API Configuration
# See SCHEDULING_SETUP.md for detailed setup instructions

# Google Calendar Service Account Credentials (JSON string)
# Get this from Google Cloud Console after creating a service account
GOOGLE_CALENDAR_CREDENTIALS={"type":"service_account","project_id":"your-project-id",...}

# Admin email (who will receive meeting invites)
ADMIN_EMAIL=admin@providencelegal.com

# Calendar ID (usually 'primary' or the admin email)
CALENDAR_ID=primary

# Time zone (IANA timezone format)
NEXT_PUBLIC_TIMEZONE=Europe/Paris
